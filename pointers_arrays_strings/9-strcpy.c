#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _strcpy - copie string
 * @dest: variable
 * @src: variable
 * Return: copie the string pointed
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
		return (dest);
}
